//插件需要依赖具体的host的版本和其他的插件，所以每个插件具有一个plugin.dps文件

文件格式如下：

{
    "packageName":"xxxx" //插件的包名
    "versionCode":"xxx" //插件的版本

     dependOnHost:{
        "packageName":"xxx"//host的包名
        "versionCode":"xxx" //host的版本
     }

     dependOnPlugins:[

        {
            "packageName":"xxx" //依赖插件的包名
            "versionCode":"xxx" //依赖插件的版本
        }

     ]


}